<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='content-type'/>
  </head>

  <body>
    <h1> Thank you for your order!</h1>
    <p>Hello <%= (@user ? @user.name : '') %>,</p>
    <p>Your payment has been successfully received and order has been placed!</p>

    <table>
      <thead>
        <tr>
          <th>Product</th>
          <th>Unit Price</th>
          <th>Quantity</th>
          <th>Price</th>
        </tr>
      </thead>
      <tbody>
        <% @order.line_items.each do |item| %>
        <tr>
          <td><%= item.product.name %></td>
          <td><%= item.item_price_cents %></td>
          <td><%= item.quantity %></td>
          <td><%= item.total_price_cents %></td>
        </tr>
        <% end %>
      </tbody>
    </table>

    <p> Total: <%= humanized_money_with_symbol @order.line_items.sum(:total_price_cents) %></p>
    <p> Thanks for using Jungle ! </p>
  </body>

</html>
